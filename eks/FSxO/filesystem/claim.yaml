apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: fsx-openzfs-pvc
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: fsx-openzfs-sc
  resources:
    requests:
      storage: 64Gi
